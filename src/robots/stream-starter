#!/usr/bin/env python
# vim: set fileencoding=utf-8:

import os
import sys
import time

import ardj.settings

if __name__ == '__main__':
    src = ardj.settings.getpath('stream/dump')
    dst = time.strftime(ardj.settings.getpath('stream/dump_rename_to'))

    if not os.path.exists(src):
        print >>sys.stderr, 'False start: %s not found.' % src
        sys.exit(1)

    os.rename(src, dst)

    from ardj import Open
    ardj = Open()
    ardj.twit(ardj.settings.get('stream/twit_begin'))
