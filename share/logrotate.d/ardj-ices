/var/run/ices/ices.log {
     daily
     missingok
     rotate 7
     postrotate
        if pgrep ardj.ices >/dev/null; then
            pkill -USR1 ardj.ices >/dev/null 2>&1
        fi
     endscript
     compress
     notifempty
}
