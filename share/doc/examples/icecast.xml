<icecast>
    <limits>
        <clients>128</clients>
        <sources>3</sources>
        <threadpool>5</threadpool>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-on-connect>1</burst-on-connect>
        <burst-size>131072</burst-size>
    </limits>

    <authentication>
        <!-- Sources log in with username 'source' -->
        <source-password>default_password</source-password>

        <!-- Admin logs in with the username given below -->
        <admin-user>admin_login</admin-user>
        <admin-password>admin_password</admin-password>
    </authentication>

    <!-- Uncomment this if you want directory listings -->
    <directory>
        <yp-url-timeout>15</yp-url-timeout>
        <yp-url>http://dir.xiph.org/cgi-bin/yp-cgi</yp-url>
    </directory>

    <!-- This is the hostname other people will use to connect to your server.
    It affects mainly the urls generated by Icecast for playlists and yp
    listings. -->
    <hostname>stream.tmradio.net</hostname>

    <!-- You may have multiple <listener> elements -->
    <listen-socket>
        <port>8180</port>
    </listen-socket>

    <listen-socket>
        <port>443</port>
    </listen-socket>

	<mount>
		<mount-name>/stream.mp3</mount-name>
		<password>stream_mp3_password</password>
	</mount>

	<mount>
		<mount-name>/music.mp3</mount-name>
		<password>music_mp3_password</password>
		<fallback-mount>/stream.mp3</fallback-mount>
		<fallback-override>1</fallback-override>
	</mount>

	<mount>
		<mount-name>/live.mp3</mount-name>
		<password>live_mp3_password</password>
		<fallback-mount>/music.mp3</fallback-mount>
		<fallback-override>1</fallback-override>
		<!-- Where to dump the stream.  The file grows, is not automatically truncated,
		     so you MUST delete it in the on-disconnect script or say good bye to your
			 free disk space. -->
        <dump-file>/radio/dump/last-live-stream.mp3</dump-file>
        <on-connect>/home/radio/bin/on-live-connected</on-connect>
        <on-disconnect>/home/radio/bin/on-live-disconnected</on-disconnect>
	</mount>

	<!-- an additional mount point for experiments -->
	<mount>
		<mount-name>/test.mp3</mount-name>
		<password>test_mp3_password</password>
		<fallback-mount>/stream.mp3</fallback-mount>
		<fallback-override>1</fallback-override>
	</mount>

    <fileserve>1</fileserve>

    <paths>
		<!-- basedir is only used if chroot is enabled -->
        <basedir>/usr/share/icecast2</basedir>

        <!-- Note that if <chroot> is turned on below, these paths must both
             be relative to the new root, not the original root -->
        <logdir>/var/log/icecast2</logdir>
        <webroot>/usr/share/icecast2/web</webroot>
        <adminroot>/usr/share/icecast2/admin</adminroot>
        <!-- <pidfile>/usr/share/icecast2/icecast.pid</pidfile> -->

        <!-- Aliases: treat requests for 'source' path as being for 'dest' path
             May be made specific to a port or bound address using the "port"
             and "bind-address" attributes.
          -->
        <!--
        <alias source="/foo" dest="/bar"/>
          -->
        <!-- Aliases: can also be used for simple redirections as well,
             this example will redirect all requests for http://server:port/ to
             the status page
          -->
        <alias source="/" dest="/status.xsl"/>
    </paths>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <!-- <playlistlog>playlist.log</playlistlog> -->
      	<loglevel>3</loglevel> <!-- 4 Debug, 3 Info, 2 Warn, 1 Error -->
      	<logsize>10000</logsize> <!-- Max size of a logfile -->
        <!-- If logarchive is enabled (1), then when logsize is reached
             the logfile will be moved to [error|access|playlist].log.DATESTAMP,
             otherwise it will be moved to [error|access|playlist].log.old.
             Default is non-archive mode (i.e. overwrite)
        <logarchive>1</logarchive>
        -->
    </logging>

    <security>
        <chroot>0</chroot>
        <!--
        <changeowner>
            <user>nobody</user>
            <group>nogroup</group>
        </changeowner>
        -->
    </security>
</icecast>
